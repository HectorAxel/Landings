$(function () {
  $(".scroll-graphic").scrollSwap({
    dataImageAttribute: "image",
    triggerFromTop: 0,
    speed: 0.5,
    noCSS: false,
    spreadDistance: "75vh",
    backgroundColor: "rgba(255,255,255,0.85)",
    width: "50%",
  });
});
